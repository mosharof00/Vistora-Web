# Auth routes

- `/login` — sign-in UI (placeholder)
- `/unauthorized` — wrong-role landing
- `/auth/confirm` — email invite / OTP / PKCE callback

Set `app_metadata.role` with the service-role client when creating users.
